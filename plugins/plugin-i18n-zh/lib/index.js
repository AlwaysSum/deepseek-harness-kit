// @dsh-kit/plugin-i18n-zh — host half.
// The 汉化 feature is browser-only; the empty apply exists so the plugin
// appears in the host Loader and its package dsh.client roster entry (the web
// half in ./client.js) is discovered and mounted in the browser.

export function apply() {
  // no host-side behavior
}
